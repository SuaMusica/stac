enum StacTextAlign { left, right, center, justify, start, end }

enum StacTextDirection { rtl, ltr }

enum StacTextOverflow { clip, fade, ellipsis, visible }

enum StacTextWidthBasis { parent, longestLine }

enum StacTextBaseline { alphabetic, ideographic }

enum StacFontWeight {
  w100,
  w200,
  w300,
  w400,
  w500,
  w600,
  w700,
  w800,
  w900,
  normal,
  bold,
}

enum StacFontStyle { normal, italic }

enum StacTextLeadingDistribution { proportional, even }

enum StacTextDecorationStyle { solid, double, dotted, dashed, wavy }
